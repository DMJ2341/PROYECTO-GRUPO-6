package com.example.cyberlearnapp.ui.theme

import androidx.compose.ui.graphics.Color

// =========================================================================
// PALETA DE COLORES CYBERLEARNAPP - ALTO CONTRASTE
// Diseñada para fondos oscuros con máxima legibilidad
// =========================================================================

// --- FONDOS ---
val BackgroundMain = Color(0xFF152342)      // Navy profundo (Fondo principal de la app)
val SurfaceCard = Color(0xFF1E2A3A)         // Gris azulado oscuro (Cards estándar)
val SurfaceElevated = Color(0xFF243447)     // Cards elevados / Contenedores secundarios
val SurfaceActive = Color(0xFF2A3F5F)       // Elementos seleccionados / activos

val TerminalBlack = Color(0xFF000000)

// --- COLORES PRIMARIOS ---
val PrimaryCyan = Color(0xFF00E5FF)         // Cian Tech (Botones, links, interacciones)
val PrimaryTeal = Color(0xFF697763)         // Teal Celestial (Elementos secundarios)
val PrimaryPurple = Color(0xFF8F5682)       // Morado Cosmic (Categorías especiales)

// --- ACENTOS ---
val AccentGold = Color(0xFFD07E47)          // Dorado Cosmic (Logros, elementos premium)
val AccentOrange = Color(0xFFB07D3A)        // Dorado más tenue (Alternativa)

// --- ESTADOS ---
val SuccessGreen = Color(0xFF10B981)        // Éxito / Correcto
val ErrorRed = Color(0xFFEF4444)            // Error / Incorrecto
val WarningOrange = Color(0xFFFFB020)       // Advertencia

// --- TEXTO (Alto Contraste) ---
val TextPrimary = Color(0xFFF3F4F6)         // Blanco roto (Máxima legibilidad)
val TextSecondary = Color(0xFF9CA3AF)       // Gris medio (Descripciones, subtítulos)
val TextTertiary = Color(0xFF6B7280)        // Gris tenue (Hints, texto auxiliar)
val TextDisabled = Color(0xFF374151)        // Muy tenue (Elementos deshabilitados)

// --- COLORES BASE DE MATERIAL (Mantener por compatibilidad) ---
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)